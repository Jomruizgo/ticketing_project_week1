The frontend status and opportunity action UI lives in the frontend/ directory, a Next.js application with React, TypeScript, Tailwind CSS, and shadcn/ui components. Create a WaitlistStatus component in components/ that accepts eventId and buyerEmail as props. On mount, it calls GET /api/waitlist/entries?eventId={id}&email={email} via a new function getWaitlistStatus(eventId, email) added to lib/api.ts. Add WaitlistStatusResponse type to lib/types.ts: { entry: WaitlistEntryResponse, opportunity?: { id: number, ticketId: number, status: string, activatedAt: string, expiresAt: string, remainingMinutes: number } }. The component renders conditionally: (a) if entry.status === "active" and no opportunity → show "En espera" badge, (b) if opportunity.status === "active" → show countdown timer (useEffect interval computing remaining time from expiresAt), show "Avanzar al pago" button, (c) if opportunity.status === "consumed" → show "Oportunidad utilizada" badge, (d) if opportunity.status === "expired" → show "Oportunidad expirada" badge. Create a useWaitlistSse(email) hook in hooks/ that connects to GET /api/waitlist/stream?email={email} using EventSource API. On "opportunity_activated" event, update state to show active opportunity. On "opportunity_expired" event, update state to show expired. The claim action calls a new function claimOpportunity(opportunityId, buyerEmail) in lib/api.ts that POSTs to /api/waitlist/opportunities/{id}/claim. Handle: 200 → redirect to payment page with ticketId, 409 → show "La oportunidad ya no está activa" toast, 403 → show error toast. The countdown timer uses a useEffect with setInterval(1000) computing Math.max(0, differenceInSeconds(expiresAt, now)) and displays MM:SS format. When timer reaches 0, disable the claim button. Integrate WaitlistStatus into the event detail page below or replacing the WaitlistEnrollForm when the buyer already has an active enrollment. Follow existing patterns from use-reservation-status.ts and use-payment-status.ts for polling and SSE hooks.
