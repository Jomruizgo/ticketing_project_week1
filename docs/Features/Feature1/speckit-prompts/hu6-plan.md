La feature de expiración de oportunidades vive en el CRUD Service (crud_service/), arquitectura hexagonal: Domain, Application, Infrastructure, Api. Usar .NET 8 con EF Core para PostgreSQL. Crear ExpireOpportunityCommand y ExpireOpportunityHandler implementando IExpireOpportunityUseCase en Application/UseCases. Contexto crítico de la implementación actual: IWaitlistOpportunityRepository (crud_service/src/CrudService.Domain/Interfaces/IWaitlistOpportunityRepository.cs) NO tiene un método FindByIdAsync — los métodos existentes son FindByWaitlistEntryIdAsync(long), FindActiveByTicketIdAsync(long), AddAsync(...), UpdateAsync(...). HU6 debe agregar FindByIdAsync(long id) al puerto y a su implementación en Infrastructure. El handler: (1) carga la oportunidad por ID desde IWaitlistOpportunityRepository.FindByIdAsync (método nuevo a crear), (2) valida que siga en estado 'active' (idempotencia: si ya expiró, ACK y retorna), (3) transiciona el estado a 'expired' usando el patrón State (WaitlistOpportunity.TransitionTo con AllowedTransitions: Active→Expired — ya definidas en crud_service/src/CrudService.Domain/Entities/WaitlistOpportunity.cs), (4) registra el motivo y la marca de tiempo de expiración, (5) guarda la oportunidad actualizada vía UpdateAsync, (6) notifica a los observers mediante IOpportunityObserver.OnOpportunityExpiredAsync — IMPORTANTE: este método NO existe aún en la interfaz IOpportunityObserver (crud_service/src/CrudService.Domain/Interfaces/IOpportunityObserver.cs solo tiene OnOpportunityActivatedAsync). HU6 debe agregar OnOpportunityExpiredAsync a la interfaz e implementarlo en OpportunityActivatedObserver (crud_service/src/CrudService.Infrastructure/Messaging/OpportunityActivatedObserver.cs) para que publique a RabbitMQ con routing key "waitlist.opportunity.expired". La notificación SSE al comprador ya está resuelta: SseNotificationConsumer (un BackgroundService en Infrastructure/Messaging, NO un observer de dominio) ya escucha la routing key waitlist.opportunity.expired y emite el evento SSE opportunity_expired — no es necesario crear ningún "SseNotificationObserver". Contexto del observer actual: AssignOpportunityHandler (HU3) inyecta un SOLO IOpportunityObserver (singular, no IEnumerable), y el DI registra OpportunityActivatedObserver como única implementación. Si HU5 ya refactoreó a IEnumerable<IOpportunityObserver>, reutilizar ese patrón; si no, el observer singular es suficiente para HU6 porque OnOpportunityExpiredAsync se agrega a la misma interfaz. (7) invoca IAssignOpportunityUseCase de HU3 (crud_service/src/CrudService.Application/UseCases/Waitlist/AssignOpportunity/IAssignOpportunityUseCase.cs — firma: HandleAsync(AssignOpportunityCommand(TicketId, EventId))) con el mismo ticketId y eventId para intentar reasignación (reutiliza la lógica FIFO existente sin duplicación), (8) si la reasignación no encuentra comprador elegible (resultado NoEligible), publica el evento ticket.returned_to_inventory (routing key: ticket.returned_to_inventory, exchange: tickets, payload: { ticketId, eventId, returnedAt }) para que ReservationService transite el ticket de released a available; esto evita el riesgo de loop que existiría si se publicara ticket.released y el propio TicketReleasedConsumer lo consumiera. Crear WaitlistOpportunityExpiredConsumer en Infrastructure/Messaging que escucha la cola q.waitlist.opportunity.expired (dead-lettered desde q.waitlist.opportunity.delay). El consumer deserializa el opportunityId del mensaje e invoca el caso de uso. ACK en éxito o idempotencia de negocio, NACK con requeue:false en fallo técnico. La topología DLX está definida en scripts/setup-rabbitmq.sh: q.waitlist.opportunity.delay (con x-message-ttl y x-dead-letter-exchange/routing-key) → q.waitlist.opportunity.expired. El TTL se establece desde la variable de entorno WAITLIST_OPPORTUNITY_TTL_MS (por defecto 900000ms = 15 min). TDD: prueba unitaria del handler para la transición active→expired, reasignación activada, sin comprador elegible devuelve la entrada al inventario, oportunidad ya expirada es idempotente. Prueba de integración con Testcontainers verificando la transición de estado y la consulta de reasignación.

Casos de prueba mínimos a implementar (de TestCases.md):
Los siguientes casos ya están definidos en docs/Features/Feature1/TestCases.md. Implementalos como mínimo. Si encontrás una inconsistencia con las reglas descritas arriba, documentala en lugar de ignorarla. Si el análisis revela escenarios no cubiertos, podés agregar casos adicionales.

- TC-HU6-01 (tipo U) — Expiración automática al alcanzar los 15 minutos
- TC-HU6-02 (tipo U) — Reasignación al siguiente comprador elegible tras expiración
- TC-HU6-03 (tipo U) — Devolución al inventario cuando no hay más compradores
- TC-HU6-04 (tipo U) — La inscripción del comprador cuya oportunidad expiró queda inactiva
- TC-HU6-05 (tipo U) — Fallo en la liberación del ticket no deja la oportunidad en estado inconsistente
