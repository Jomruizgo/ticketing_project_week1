The frontend enrollment UI lives in the frontend/ directory, a Next.js application with React, TypeScript, Tailwind CSS, and shadcn/ui components. Modify the event detail page (app/events/[id]/page.tsx or the relevant event detail component) to conditionally render a waitlist enrollment form when the event has no available tickets and the event date has not passed. Create a WaitlistEnrollForm component in components/ that accepts eventId as prop: it has an email input field and a submit button labeled "Unirse a la lista de espera". On submit, it calls the CRUD Service POST /api/waitlist/entries endpoint via a new function enrollInWaitlist(eventId, buyerEmail) added to lib/api.ts. Handle responses: 201 → show success message and display enrollment as active, 409 → show "Ya tienes una inscripción activa" message, 422 → show "La lista de espera ya cerró" message. Add WaitlistEntryResponse type to lib/types.ts: { id: number, eventId: number, buyerEmail: string, status: string, enrolledAt: string }. The event detail page logic: if availableTickets > 0, show existing purchase flow; if availableTickets === 0 and event date is in the future, show WaitlistEnrollForm; if event date is in the past, show "Lista de espera cerrada". Use the existing API base URL configuration from lib/api.ts. Follow the existing component patterns: shadcn/ui Card, Button, Input components, toast notifications for feedback. No new backend changes — this consumes the API built in HU1.
